# /aagm-push
Inspect remote and local commits. Under default CONTROLLED autonomy, push is ASK. Recommend push at durable checkpoints; never assume remote exists. Report exactly what will be synchronized.
