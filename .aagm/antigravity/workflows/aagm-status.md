# /aagm-status
Read authoritative project state, synchronize Dashboard when needed, verify consistency, present concise Sponsor status and open `docs/aagm/PROJECT_DASHBOARD.html` when the IDE/platform permits. Never treat HTML as source of truth.
