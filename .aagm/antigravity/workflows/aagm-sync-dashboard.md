# /aagm-sync-dashboard
Regenerate the Dashboard from authoritative Git-tracked state, set sync timestamp/status, verify required sections. This workflow changes presentation metadata only; it must not invent project facts.
