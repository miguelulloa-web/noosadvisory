# /aagm-run-qa
Run/coordinate minimum sufficient stack-aware QA: feature checks plus relevant regression/protected-baseline checks, CI/build/contract/security checks when applicable. QA cannot approve its own implementation. Record empirical evidence.
